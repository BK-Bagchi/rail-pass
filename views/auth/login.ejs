<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rail Pass Login</title>
    <link rel="stylesheet" href="../css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <script src="../js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="container mt-5" style="height: 50vh;">
        <h2 class="text-center mb-4">Login to Rail Pass</h2>
        <form action="/auth/login" method="POST" class="p-4 shadow rounded bg-light" style="max-width: 500px; margin: auto;">
            <div class="mb-3">
                <label for="email" class="form-label fw-bold">Email</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="Enter your email" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label fw-bold">Password</label>
                <input type="password" class="form-control" id="password" name="password" placeholder="Enter your password" required>
                
                <% if (userMissMatch || passwordMissMatch) { %>
                    <p class="text-danger mt-1"><%= userMissMatch || passwordMissMatch%></p>
                <% }%>
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-primary btn-lg">Login</button>
            </div>
        </form>
        <p class="text-center mt-3">Don't have an account? <a href="/auth/register" class="text-primary">Register here</a></p>
    </div>
    <%- include('../partials/footer') %>
</body>
</html>