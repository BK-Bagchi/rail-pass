<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Success</title>
    <link rel="stylesheet" href="../../css/bootstrap.min.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <script src="../../js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <%- include('../partials/navbar') %>
    <div class="container py-5">
    <div class="text-center mb-5">
        <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
        <h1 class="fw-bold text-success mt-3">Payment Successful!</h1>
        <p class="text-muted">Your ticket has been confirmed. Please find your booking details below.</p>
    </div>

    <div class="card shadow-lg border-0 rounded-3">
        <div class="card-body p-4">
        <h4 class="fw-bold mb-4 text-primary">Passenger & Booking Info</h4>
        <div class="row mb-3">
            <div class="col-md-6">
            <p><strong>Name:</strong> <%= user.firstName %> <%= user.lastName %></p>
            <p><strong>PNR:</strong> <%= bookingInfo._id %></p>
            <p><strong>Mobile:</strong> <%= user.mobile || "013xxxxxxxx"%></p>
            <p><strong>Confirmation Date:</strong> <%= new Date(bookingInfo.confirmationDate).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }) %></p>
            </div>
            <div class="col-md-6">
            <p><strong>Journey Date:</strong> <%= new Date(bookingInfo.journeyDate).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }) %></p>
            <p><strong>From Station:</strong> <%= bookingInfo.fromStation %></p>
            <p><strong>Departure Time:</strong> <%= bookingInfo.departureTime %></p>
            <p><strong>To Station:</strong> <%= bookingInfo.toStation %></p>
            <p><strong>Arrival Time:</strong> <%= bookingInfo.arrivalTime %></p>
            </div>
        </div>
        <hr>
        <h4 class="fw-bold mb-4 text-primary">Seat & Fare Info</h4>
        <div class="row">
            <div class="col-md-6">
            <p><strong>Seat Type:</strong> <%= bookingInfo.seatClass %></p>
            <p><strong>Total Seats:</strong> <%= bookingInfo.seatNumber.length %></p>
            <p><strong>Seats:</strong> <%= bookingInfo.seatNumber.join(", ") %></p>
            </div>
            <div class="col-md-6">
            <h5 class="fw-bold text-success">Total Fare: ৳ <%= bookingInfo.totalFare %></h5>
            </div>
        </div>
        </div>
    </div>
    <div class="text-center mt-4">
        <a href="/" class="btn btn-outline-primary btn-lg px-4 me-2">Go Home</a>
        <a href="/booking/downloadTicket/<%= bookingInfo._id %>" class="btn btn-success btn-lg px-4">Download Ticket</a>
    </div>
    </div>
    <%- include('../partials/footer') %>
</body>
</html>